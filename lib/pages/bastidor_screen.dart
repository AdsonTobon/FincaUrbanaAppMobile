import 'package:flutter/material.dart';

class BastidorScreen extends StatelessWidget {
   
  const BastidorScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('BastidorScreen'),
      ),
    );
  }
}