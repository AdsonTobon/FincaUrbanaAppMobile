import 'package:flutter/material.dart';

class CctScreen extends StatelessWidget {
   
  const CctScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('CctScreen'),
      ),
    );
  }
}